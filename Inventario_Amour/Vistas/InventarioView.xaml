<UserControl x:Class="Inventario_Amour.Vistas.InventarioView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Inventario_Amour.Vistas"
      mc:Ignorable="d" 
      Height="464" Width="800"             
      Background="LightBlue" Margin=" 5 5 5 5">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <TextBox Text="{Binding FiltroNombre, UpdateSourceTrigger=PropertyChanged}" Margin="10,10,0,0" Width="140"  Style="{StaticResource MaterialDesignFloatingHintTextBox}" Grid.Column="0" Grid.Row="0" materialDesign:HintAssist.Hint="Nombre" HorizontalAlignment="Left" VerticalAlignment="Center"></TextBox>
        <Button Style="{StaticResource MaterialDesignRaisedLightButton}" 
            Content="Consultar" Grid.Row="0" Grid.Column="1" 
            Command="{Binding ConsultarProductosCommand}"
            HorizontalAlignment="Left"
            FontSize="13"  Margin="0,10,0,10" Cursor="Hand">
        </Button>
        <Label HorizontalAlignment="Left" Margin="20,10,0,0" Grid.Row="0" Grid.Column="4" Grid.ColumnSpan="2" VerticalAlignment="Top"
            FontWeight="Bold" FontSize="15"  Content="Información Inventario AMOUR" ></Label>
        <DataGrid ItemsSource="{Binding Productos}" AutoGenerateColumns="False" CanUserAddRows="False" Grid.ColumnSpan="5" IsReadOnly="True"
        Grid.Row="2" Background="LightBlue">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
            <DataGridTextColumn Header="Entradas" Binding="{Binding TotalEntradas}" Width="*"/>
            <DataGridTextColumn Header="Salidas" Binding="{Binding TotalSalidas}" Width="*"/>
            <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="*"/>
        </DataGrid.Columns>
    </DataGrid>
    </Grid>
</UserControl>
